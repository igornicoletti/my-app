import { CheckSquareOffsetIcon, HouseSimpleIcon, UserIcon, type Icon } from '@phosphor-icons/react'

export const routeIcon: Record<string, Icon> = {
  dashboard: HouseSimpleIcon,
  users: UserIcon,
  tasks: CheckSquareOffsetIcon,
}
