import {
  HouseSimpleIcon,
  NotepadIcon,
  UserIcon,
  type Icon
} from '@phosphor-icons/react'

export const ICON: Record<string, Icon> = {
  dashboard: HouseSimpleIcon,
  user: UserIcon,
  tasks: NotepadIcon,
}
